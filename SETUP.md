# 台风救援小程序 — 从零到跑完整指引

---

## 第 1 步：注册小程序账号

1. 打开 https://mp.weixin.qq.com/ → 点 **"立即注册"**
2. 选择 **"小程序"** 类型
3. 填写邮箱、密码，按提示完成注册
4. 登录后，左侧菜单 → **"开发管理"** → **"开发设置"**
5. 找到 **AppID（小程序 ID）**，复制下来（一串 `wx` 开头的字符串）

---

## 第 2 步：下载安装微信开发者工具

1. 打开 https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
2. 选择 **Windows 64位** 版本，下载安装
3. 安装时全部默认选项，一路"下一步"即可

---

## 第 3 步：导入项目

1. 打开微信开发者工具 → 点 **"导入项目"**
2. 填写：

   | 字段 | 填写内容 |
   |------|---------|
   | 项目目录 | `D:\AI\typhoon-relief`（点"…"按钮选择） |
   | AppID | 粘贴第 1 步复制的 AppID |
   | 后端服务 | **选"微信云开发"** |

3. 点 **"确定"**

---

## 第 4 步：开通云开发

1. 进入开发者工具界面后，顶栏点 **"云开发"** 按钮（☁️ 云朵图标）
2. 弹出窗口 → 点 **"开通"**
3. 环境名称填：`typhoon-relief`（随便填）
4. 等待 1-2 分钟初始化完成
5. 初始化后不要关闭云开发控制台

---

## 第 5 步：创建数据库集合

在云开发控制台（刚刚打开的窗口）中：

1. 左侧菜单选 **"数据库"**
2. 点 **"新建集合"** → 输入 `typhoons` → 确定
3. 再次点 **"新建集合"** → 输入 `sos_signals` → 确定

---

## 第 6 步：给 sos_signals 建索引

在云开发控制台的数据库页面：

1. 点进 `sos_signals` 集合
2. 切换到 **"索引管理"** 标签
3. 点 **"新建索引"**，按以下列表逐个添加：

| 字段名 | 排序 | 类型 |
|--------|------|------|
| `location_detail.lat` | 升序 | 普通索引 |
| `location_detail.lng` | 升序 | 普通索引 |
| `status` | 升序 | 普通索引 |
| `expires_at` | 升序 | 普通索引 |
| `created_at` | **降序** | 普通索引 |

---

## 第 7 步：上传云函数

回到开发者工具主界面（不要用云开发控制台了）

在左侧文件树中：

### 7.1 装依赖（如果还没装）

对着 `cloudfunctions/setupDB` 目录 **右键 → 在终端中打开**

依次执行以下命令（每个执行完再执行下一个）：

```bash
cd D:\AI\typhoon-relief\cloudfunctions\syncTyphoon
npm install

cd D:\AI\typhoon-relief\cloudfunctions\publishSOS
npm install

cd D:\AI\typhoon-relief\cloudfunctions\nearbySOS
npm install

cd D:\AI\typhoon-relief\cloudfunctions\updateSOS
npm install

cd D:\AI\typhoon-relief\cloudfunctions\setupDB
npm install
```

> 我已在后台帮您完成了前 4 个云函数的 npm install。
> 您只需装 setupDB 一个即可（或者也可以不装，上传时选"云端安装依赖"）。

### 7.2 上传部署

对着每个云函数文件夹 **分别右键 → "上传并部署"（选"云端安装依赖"）**：

1. `cloudfunctions/setupDB/` → 右键 → 上传并部署（选云端安装依赖）
2. `cloudfunctions/syncTyphoon/` → 右键 → 上传并部署（选云端安装依赖）
3. `cloudfunctions/publishSOS/` → 右键 → 上传并部署（选云端安装依赖）
4. `cloudfunctions/nearbySOS/` → 右键 → 上传并部署（选云端安装依赖）
5. `cloudfunctions/updateSOS/` → 右键 → 上传并部署（选云端安装依赖）

每次看到提示 **"部署成功"** 再上传下一个。

---

## 第 8 步：运行初始化脚本

1. 在开发者工具顶栏，点 **"编译"** 旁边的下拉箭头 → 选 **"编译模式"** 下的 **"添加编译模式"**
2. 模式名称填：`初始化数据库`
3. **启动命令**选择：`在云函数中调用` → 选择 `setupDB`
4. 点 **"确定"**
5. 点 **"编译"** 运行
6. 控制台出现 `成功 X 项，失败 0 项` 说明初始化成功

> 如果不想设置编译模式，也可以直接点云开发控制台 → 数据库，确认 typhoons 和 sos_signals 两个集合已经存在就行。

---

## 第 9 步：启动项目

1. 回到开发者工具
2. 顶栏点 **"编译"**（或按 `Ctrl + B`）
3. 左侧模拟器应该显示首页地图

---

## 第 10 步：验证功能

**验证 1 — 台风追踪**
- 首页地图显示以 **上海为中心** 的地图
- 可见 **蓝色实线**（台风历史路径）和 **红色虚线**（预测路径）
- 可见 **3 个半透明圆环**（7级/10级/12级风圈）
- 底部显示台风名称"巴威"和风速、气压等信息

**验证 2 — 发布求救**
- 底部 tab 点 **"救援互助"**
- 点右下角红色 **SOS 按钮**
- 授权定位 → 拍一张照片（或选一张相册图片）
- 选一个需求类型 → 填写描述 → 点"发布求救信号"
- 提示"求救已发出" → 自动返回救援页面

**验证 3 — 查看和接单**
- 救援页面地图上出现你发的 **红色求救标记**
- 点标记 → 进入详情
- 点 **"导航前往"** → 调起微信地图导航（验证跳转）
- 点 **"我来救援"** → 状态变为"救援中"

**验证 4 — 科普专栏**
- 底部 tab 点 **"防灾科普"**
- 三个分类 Tab：台风前 / 台风中 / 台风后
- 点任意文章 → 阅读完整内容

---

## 还没完成的功能（代码已写，后续迭代）

| 功能 | 说明 |
|------|------|
| 腾讯地图 Key | `sos-publish.js` 中逆地理编码需要替换真实腾讯地图 key |
| 真实台风数据 | `syncTyphoon/index.js` 中 `fetchExternal()` 需要替换真实 API 地址 |
| 详情页联系求助者 | MVP 阶段只显示"功能开发中"，后续通过云函数获取完整号码 |
| 个人中心查看记录 | "我发布的求救"等页面显示"功能开发中" |
| 预警推送 | 台风路径进入关注范围时发订阅消息 |

---

## 常见问题

**Q：地图一片空白 / 加载不出来**
A：在云开发控制台 → 数据库确认 `typhoons` 集合有没有数据。没有的话调用一次 `syncTyphoon` 云函数（在云开发控制台 → 云函数 → 点 syncTyphoon → 点"测试"）。

**Q：tabBar 图标显示灰色方块**
A：`miniprogram/images/` 下的 6 个图标是占位方块的，正式上线前换成你的图标即可（48x48 PNG）。

**Q：点发布求救没反应**
A：确认 `publishSOS` 云函数已上传部署。检查云开发控制台 → 云函数 → publishSOS 是否显示"已部署"。

**Q：个人中心点登录没反应**
A：微信官方策略调整，`wx.getUserProfile` 接口需要审核。可改为用 `wx.getUserInfo` 或等上线后更新。

---

原文：`D:\AI\typhoon-relief\PRD.md`  
代码变更可随时问我。
